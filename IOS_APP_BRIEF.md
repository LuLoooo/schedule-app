# iOS 原生 App 开发交接文档 —「日程管家」

> 交接目的：基于已有的网页版（PWA），开发一个 **iOS 原生 App**。
> 网页版已完成并上线，功能已被用户验证满意。iOS 版要做的是：**用原生技术完整复刻这套功能和体验**，并利用原生能力做得更好。

---

## 一、项目背景

用户已有一个纯前端日程管理网页应用（Vue3 CDN + localStorage，无后端），核心卖点是**中文自然语言输入**：用户像说话一样输入一句话，应用自动解析出日程和行车里程。

- **源代码（务必先通读）**：`/Users/loogu/WorkBuddy/2026-07-20-00-21-11/`
- **线上演示（可直接体验）**：https://luloooo.github.io/schedule-app/
- **GitHub 仓库**：https://github.com/LuLoooo/schedule-app
- 代码量约 6200 行：`index.html`(730) / `css/styles.css`(1767) / `js/app.js`(1499) / `js/nlp.js`(1086) / `js/storage.js`(505) / `js/exporter.js`(432) / `js/scheduler.js`(221)

**建议阅读顺序**：本文档 → `js/nlp.js`（核心，解析规则全在这）→ `js/storage.js`（数据模型）→ `js/app.js`（交互逻辑）→ 线上实际体验一遍。

---

## 二、功能清单（iOS 版必须全部覆盖）

### 1. 中文自然语言输入（核心中的核心）
- 输入一句话 → 自动解析出：日期、时间段、事件标题、分类、地点、里程。
- 例："明天下午2点去南山拍摄，开了35公里" → 生成 1 条日程（明天 14:00，拍摄，地点南山）+ 1 条里程记录（35km），**二者关联，删日程连带删里程**。
- 一句话含多个事件必须拆条："上午拍摄、下午剪辑、晚上健身" → 3 条独立日程。分隔依据：逗号、顿号、空格、句中时间词开头。
- 支持删除命令："删掉明天的会议"；支持排除式删除："除了拍摄以外都删掉"。
- 中文数字要认（"两点半"=14:30，"三十五公里"=35km）。
- **用户红线**：① 自然语言连接词（然后/接着/我要去 等）绝不能进日程标题，标题只归纳核心事件；② 操作一步到位，不要多步确认（危险的删除操作保留一次确认即可）。
- iOS 版解析逻辑可直接把 `js/nlp.js` 的正则/规则翻译成 Swift，也可以嫁接系统能力（如 NSDataDetector、语音输入），但**行为必须与网页版一致**，nlp.js 是唯一权威规格。

### 2. 日程管理
- 日 / 周 / 月 三视图。时间段事件用**连续色块矩形**表示（用户明确反对箭头等装饰元素）。
- 手机上周视图 7 天必须一屏放下，**不允许横向滑动**；月视图格子固定尺寸，字多自动换行截断，点进去看全部。
- 事件可点击编辑：标题、日期、起止时间、分类、地点。
- 智能排程建议（`js/scheduler.js`）：找空闲时段、冲突提醒。

### 3. 分类系统
- 内置分类：策划、拍摄、剪辑、摸鱼、其他（等，见 storage.js 默认值）。
- 用户可**自定义增删改**分类（名称+颜色+关键词）；输入时按关键词自动归类。
- 删除分类时必须弹选项：让用户选择该分类下日程归入哪个其他分类。

### 4. 地点系统
- 说话时自动语义提取地点（"在/到/去/从/往 + 地点"）。
- **常用地点管理**：可增、删、改名；改名后所有引用该地点的日程和里程同步更新。
- 说到已保存的常用地点时**优先精确匹配**（最长优先）自动归到该地点。
- 编辑事件时地点用**下拉选择**：（无地点）/ 常用地点列表 / ＋新增地点。

### 5. 行车里程记录
- 独立的"里程记录"模块（与日程管理平级 Tab）。
- 记录：日期、公里数、备注、关联日程。
- 统计：总里程、日均里程、趋势图表。
- **油费换算**：油费 = 里程 × 油价 × 油耗 ÷ 100，油价/油耗在设置中可调。

### 6. 多账户
- 本地多账户，登录界面选择/新建账户（**登录界面不放改名功能**，改名只在进入后右上角）。
- 每个账户数据完全隔离。
- 视图范围切换：「本账户 / 全部账户」，全部账户模式下合并展示所有账户日程并带账户彩色标签，可跨账户编辑。

### 7. 数据
- **全部数据只存本地，无后端、无云、无登录注册**。iOS 版建议 Core Data / SwiftData / SQLite，按账户隔离。
- 用户核心诉求：**不卸载 App 数据就永远在**。
- 导出：JSON / CSV / HTML；导入：JSON（按 ID 合并、不覆盖已有）。iOS 版请保持**与网页版 JSON 格式互通**，用户可能要把网页版数据迁到 App（格式见 `js/exporter.js` 与 `js/storage.js` 的 import/export 部分）。

### 8. 数据模型（storage.js 为准，关键字段）
```
Event:   { id, title, date(YYYY-MM-DD), startTime(HH:mm), endTime, allDay,
           category, color, location, note, mileageId(关联里程), createdAt }
Mileage: { id, date, km, note, eventId(关联日程), createdAt }
Account: { id, name }
Settings:{ oilPrice, oilConsumption, categories[{name,color,keywords[]}], locations[] }
存储命名空间：schedule_manager_events__<accountId> 等，按账户隔离
```

---

## 三、设计要求（用户明确偏好，不要偏离）

- **现代高端设计**：Notion / Linear / Attio 级别的干净利落，拒绝过时的安卓风/拟物风。
- **浅色简约清爽主题**；主色调：莫兰迪蓝绿或低饱和蓝紫。
- 卡片：轻阴影 + 圆角，无硬边框。
- 无衬线中文优先字体（iOS 上苹方即可）。
- 涉及涨跌/增减的数字：**中国习惯，红涨绿跌**。
- 大触摸目标、safe-area 适配、防误触。
- 日历色块内不要装饰性箭头，直观的连续矩形表示时间区间。

## 四、用户沟通偏好（重要）

- 中文交流。用户会先给完整需求再逐条反馈修改意见，决策快，说"可以/是的"就是确认。
- 用户不写代码，**交付前先给设计稿/效果图或直接可跑的东西**，别甩技术名词。
- 每次改动要自己测好再交付，用户只验收效果。
- 对成本敏感：方案别绕弯路、别重复消耗。

## 五、iOS 版技术建议（供参考，可自行决策）

- Swift + SwiftUI（最贴合上述设计要求），最低支持 iOS 16+。
- 数据层 SwiftData 或 Core Data；NLP 层将 `js/nlp.js` 规则移植为 Swift（约 1000 行正则+规则，无外部依赖，可完整移植）。
- 可加分的原生能力：Siri/快捷指令添加日程、语音输入、本地通知提醒、桌面小组件（当日日程）、与系统日历(EventKit)可选同步。
- 注意：用户没有 Apple 开发者账号的信息，真机安装/上架方式请与用户确认（侧载 TestFlight / 上架 App Store / 仅模拟器演示）。

## 六、待确认问题（接手后先问用户）

1. iOS 版数据是否需要从网页版导入？（建议做 JSON 导入兼容）
2. 是否需要本地通知提醒功能（网页版没有，原生的天然优势）？
3. 安装方式：有没有 Mac + Xcode 环境？有没有 Apple ID / 开发者账号？
